<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      A bare Material3 theme. The Compose UI supplies its own MaterialTheme at
      runtime; this XML theme only needs to exist (and be Material3-derived) so
      the Activity has a valid base theme before setContent runs, and so the
      system bars use sensible defaults.
    -->
    <style name="Theme.PhantomDemo" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
