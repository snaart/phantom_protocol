<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Open a TCP socket to the phantom-server. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Observe Wi-Fi <-> cellular changes to trigger a reconnect-with-0-RTT (migrate() is a no-op over the TCP FFI surface). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Hold the session alive in a foreground service (survives Doze). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Required to post the foreground-service notification on Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Used by the battery-optimization-exemption prompt. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhantomDemo">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PhantomDemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".PhantomSessionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>

</manifest>
