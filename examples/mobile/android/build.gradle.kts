// Top-level build file. The plugin versions are declared here with `apply false`
// and applied in the :app module's build script.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
